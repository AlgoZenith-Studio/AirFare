# tests

Contract tests asserting API responses match TRD Part D exactly (field names, null behavior,
error shapes), plus unit tests for the econometrics module. Index math must be independently
verifiable; this is the auditability guarantee the whole product rests on.
