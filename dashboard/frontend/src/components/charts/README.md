# components/charts

ChartFrame wrapper + chart implementations (index time series, attribution waterfall, base-vs-total
line+band, fee composition stacked bar, lead-time heatmap, elasticity curve, carrier grouped bar,
component stacked area, coverage trend, source agreement heatmap, route sparkline).

Hard rule: zero dual-axis charts. Every chart ships an accessible DataTable toggle.
