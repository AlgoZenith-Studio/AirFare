# lib/firebase

Firebase client SDK initialization: Auth (RBAC via custom claims VIEWER/ANALYST/ADMIN) and
Firestore onSnapshot listeners for live source health, circuit-breaker state, and DGCA surge alerts.
