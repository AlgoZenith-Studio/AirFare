# lib/api

Typed REST client for the FastAPI backend (/api/v1). Mirrors TRD Part D contracts exactly,
including the standard data/meta envelope and mandatory quality-metadata fields on every
index-bearing response.
